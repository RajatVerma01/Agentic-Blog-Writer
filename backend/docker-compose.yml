version: '3.8'

services:
  agentic-blog-writer:
    build:
      context: .
      dockerfile: Dockerfile
    image: agentic-blog-writer:latest
    container_name: agentic-blog-writer
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
